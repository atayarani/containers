servicePorts:
  - name: http
    protocol: TCP
    port: 8989
    targetPort: 8989
replicaCount: 1

image:
  repository: lscr.io/linuxserver/sonarr
  pullPolicy: IfNotPresent
  tag: latest

ingressEnabled: true
